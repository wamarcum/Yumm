<%= title("Tables", @table.number) %>
<h2>Table Number: <%= @table.number %></h2>
<h4>Guests: <%= @table.guests %></h4>

<%= link_to "Edit Table", edit_table_path(@table) %>
<%= link_to "Delete Table", table_path(@table),
                            method: :delete,
                            data: { confirm: 
                                    "Are you sure you want to delete this Table?"
                                  }
%>

<h3>Tickets</h3>
<%= link_to "New Ticket", new_table_ticket_path(@table) %>
<ul id='tickets'>
  <% @table.tickets.each do |ticket| %>
    <li>
      #<%= ticket.id %> - 
      <%= link_to ticket.title, [@table, ticket] %>
    </li>
  <% end %>
</ul>

